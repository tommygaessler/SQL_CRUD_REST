CREATE TABLE students (
  id numeric,
  first_name text,
  last_name text,
  jobs_applied text
)

CREATE TABLE teachers (
  id numeric,
  first_name text,
  last_name text,
)

CREATE TABLE classes (
  id numeric,
  subject text
)
